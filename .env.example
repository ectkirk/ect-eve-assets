# EVE Online SSO Client ID (required for development)
# Register at https://developers.eveonline.com/
# Callback URL: http://localhost:52742/callback
#
# For releases, EVE_CLIENT_ID is embedded at build time from GitHub secrets.
# See CLAUDE.md "EVE SSO Setup" section.
EVE_CLIENT_ID=your-client-id-here

# API Key for edencom.net ref API authentication (optional for dev, required for release)
# For releases, REF_API_KEY is embedded at build time from GitHub secrets.
REF_API_KEY=

# Discord webhook for bug reports (optional for dev, required for release)
# For releases, DISCORD_BUG_WEBHOOK is embedded at build time from GitHub secrets.
DISCORD_BUG_WEBHOOK=
